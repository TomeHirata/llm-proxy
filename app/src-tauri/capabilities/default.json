{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for llmproxy desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:tray:default"
  ]
}
